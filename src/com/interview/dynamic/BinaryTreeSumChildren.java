package com.interview.dynamic;

/**
 * Given a Binary Tree, For each node, add sum of all the nodes in its hierarchy to its value.
 * Below picture shows a sample input and output.
 *
 */
public class BinaryTreeSumChildren {
//    void addChildSum(Node *root) {
//        if (root == NULL) return; // Terminating cond.
//
//        // Compute for Left Sub Tree
//        addChildSum(root -> left);
//
//        // Compute for Right Sub Tree
//        addChildSum(root -> right);
//
//        int finalSum = root -> data;
//        if (root -> left != NULL)
//            finalSum += root -> left -> data;
//
//        if (root -> right != NULL)
//            finalSum += root -> right -> data;
//
//        root -> data = finalSum;
//    }
}