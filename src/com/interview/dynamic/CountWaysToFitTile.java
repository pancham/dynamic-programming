package com.interview.dynamic;

/**
 * Given an empty plot of size 2 x n. We want to place tiles such that the entire plot is covered.
 * Each tile is of size 2 x 1 and can be placed either horizontally or vertically.
 *
 * Solution is same as Fibonacci series except terminating conditions
 */
public class CountWaysToFitTile {
    // TODO:
    // 8.2: If size of the plot in Example 8.2 is changed to 3*n, then what changes do we need to make in the solution?
    //

//    int countWays(int n){
//        // terminating conditions
//        if(n == 0) { return 0; }
//        if(n == 1) { return 1; }
//        if(n == 2) { return 2; }
//        return countWays(n-1) + countWays(n-2);
//    }
}